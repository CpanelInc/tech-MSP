severity = 4
verbose  = [%p] Line %l Column %c - %m - %e. (Severity: %s)\n

[InputOutput::RequireBriefOpen]
lines = 30

[-Modules::ProhibitMultiplePackages]
# Must silence because MSP is a modulino.

[-Subroutines::RequireFinalReturn]
# MSP uses a large number of subs that do not need an explicit return because their return value is not used.

[-Modules::RequireExplicitInclusion]
# MSP makes efforts to include and use some modules that may or may not exist on a system, so they don't get         imported in the usual way

[-Subroutines::ProhibitCallsToUndeclaredSubs]
# MSP makes efforts to include and use some modules that may or may not exist on a system, so they don't get         imported in the usual way

[-Subroutines::ProhibitCallsToUnexportedSubs]
